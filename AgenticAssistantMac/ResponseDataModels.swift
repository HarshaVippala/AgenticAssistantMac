import Foundation

// Placeholder for complex data types until they are defined

class StarResponsePlaceholder {
    // Define properties for STAR response (Situation, Task, Action, Result)
    var situation: String
    var task: String
    var action: String
    var result: String

    init(situation: String = "", task: String = "", action: String = "", result: String = "") {
        self.situation = situation
        self.task = task
        self.action = action
        self.result = result
    }
}

class DiagramPlaceholder {
    // Define properties for a diagram (e.g., ASCII, SVG data, or a path)
    var representation: String

    init(representation: String = "") {
        self.representation = representation
    }
}